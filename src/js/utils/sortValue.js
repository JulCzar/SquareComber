/**
 * Function that generate integers till a given maximum
 * @param {Number} amplitude Maximum value to return. Won't be included in the results! default = 2
 */
export const sortValue = (amplitude = 2) => Math.floor(Math.random() * amplitude)